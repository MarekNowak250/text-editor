<Window x:Class="TextEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="Text editor - PHX edition"
        mc:Ignorable="d" 
        Loaded="Window_Loaded"
        Height="450" Width="800" 
        PreviewTextInput="Window_PreviewTextInput" 
        PreviewKeyDown="Window_PreviewKeyDown">
    <Grid Focusable="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.1*"/>
            <RowDefinition Height="0.9*"/>
        </Grid.RowDefinitions>
        <Grid Background="#EEE" Height="30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.8*"/>
                <ColumnDefinition Width="0.1*"/>
                <ColumnDefinition Width="0.1*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Name="title" Text="file.txt" Padding="8"/>
            <Button Grid.Column="1" Click="Save_Click" Focusable="False">Save</Button>
            <Button Grid.Column="2" Click="Load_Click" Focusable="False">Load</Button>
        </Grid>
        <Canvas x:Name="Main" Background="AliceBlue" Grid.Row="1">

        </Canvas>
    </Grid>

</Window>
